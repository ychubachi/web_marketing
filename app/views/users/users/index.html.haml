%h2 メニュー
%ul
  %li
    = link_to 'リダイレクト一覧', admin_redirections_path
  %li
    = link_to '顧客一覧', admin_customers_path

%h2 Users
- @users.each do |user|
  %br/
  #{link_to user.email, users_user_path(user)} signed up #{user.created_at.to_date}
