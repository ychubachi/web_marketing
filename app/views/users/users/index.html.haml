%h2 メニュー
%ul
  %li
    %a{href: '/admin/redirections'} リダイレクト一覧
  %li
    %a{href: '/admin/customers'} 顧客一覧

%h2 Users
- @users.each do |user|
  %br/
  #{link_to user.email, users_user_path(user)} signed up #{user.created_at.to_date}
